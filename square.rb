class Square < Container
  #def package_inside
  #  fields.select do |field|
  #    next unless field.value
  #    all_the_possible_values.count(field.value) == 2 && all_the_possible_values.count(field.value) == 3 &&
  #      fields.select { ||}
  #  end
  #end
end
